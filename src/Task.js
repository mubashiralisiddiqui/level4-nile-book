import React ,{Component} from "react"
import React from 'react';
import ReactDOM from 'react-dom';

var Task = React.createClass({
    render:function(){
        return (
            <div>
            <h1 className = "jumbotron btn-primary">Taks Page</h1>
            </div>
        )
    }
})

module.exports = Task;